import { HexBase64BinaryEncoding } from 'crypto';

export class Produtos {
  chaveProduto: number;
  // ChaveCategoria: number;
  nome: string;
  descricao: string;
  preco: number;
  nomeImagem: string;
}
